# Claude Monitor Configuration
#
# List your accounts here (value can be anything, just needs to exist)
# Then run: npm run login <account-name> for each one

ACCOUNT_PERSONAL=1
ACCOUNT_WORK=1

# Optional: custom profiles directory (default: ./profiles)
# PROFILES_DIR=./profiles
